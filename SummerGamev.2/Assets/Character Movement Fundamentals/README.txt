Scripts
Prefabs
tags
layers
-For some reason the second turret spawns irregularly
-IMPORTANT the player must get the instance of the game manager before the scene starts, or else the don't destroy on load method will hide the gamemanager from any FIND methods called after scene initialization.
-Stats system I don't get
-Dont have player health system, LevelingSystem and TalentSystem stats routed to main stats manager {I presume it is PlayerStatsManager);